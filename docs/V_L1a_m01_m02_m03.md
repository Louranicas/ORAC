# V_L1a: D7 Verification — m01_core_types, m02_error_handling, m03_config

> **Verified:** 2026-03-25 | **Method:** 3 subagent parallel source read + claim-by-claim comparison
> **Source:** `src/m1_core/m01_core_types.rs` (1129 LOC), `m02_error_handling.rs` (596 LOC), `m03_config.rs` (654 LOC)
> **D7 claims from:** `docs/D7_MODULE_PURPOSE_GUIDE.md` Layer 1 sections

---

## m01_core_types (1,129 LOC)

### Types (14 claimed)

| # | D7 Claim | Verdict | Detail |
|---|----------|---------|--------|
| 1 | `PaneId(String)` newtype | **MATCH** | Line 29. Derives Debug, Clone, Default, PartialEq, Eq, PartialOrd, Ord, Hash, Serialize, Deserialize. |
| 2 | `TaskId(String)` UUID v4 newtype | **MATCH** | Line 65. Uses `uuid::Uuid::new_v4()` in `new()`. |
| 3 | `Point3D { x, y, z }` Copy, 24 bytes | **MATCH** | Lines 107-115. Derives Clone, Copy. 3xf64 = 24 bytes. |
| 4 | `SphereMemory` | **MATCH** | Lines 233-248. Fields: id, position, activation, tool_name, summary, timestamp, confidence. |
| 5 | `Buoy` | **MATCH** | Lines 271-286. Fields: position, home, activation_count, influence_radius, boost_multiplier, learning_rate, label. |
| 6 | `PaneSphere` | **MATCH** | Lines 635-662. Has id, persona, status, phase, frequency, buoys, memories + extra fields (opt_out_hebbian, activity_30s, total_steps, receptivity, work_signature, field_context). D7 accurate but incomplete. |
| 7 | `OrderParameter { r, psi }` | **MATCH** | Lines 394-399. |
| 8 | `PaneStatus`: Idle, Working, Blocked | **MISMATCH** | Lines 332-342. Has **4** variants: Idle, Working, Blocked, **Complete**. D7 omits Complete. |
| 9 | `FleetMode`: Solo, Small, Medium, Large, Massive | **MISMATCH** | Lines 501-511. Actual **4** variants: Solo, Pair, Small, Full. D7 fabricates Medium/Large/Massive. |
| 10 | `RTrend`: Stable, Rising, Falling | **MATCH** | Lines 479-487. Variants Rising, Falling, Stable (default). |
| 11 | `FieldAction`: Steady, BoostK, ReduceK, KickPhases, IdleFleet | **MISMATCH** | Lines 442-460. Actual **8** variants: Stable, NeedsCoherence, NeedsDivergence, HasBlockedAgents, IdleFleet, FreshFleet, Recovering, OverSynchronized. Only IdleFleet matches. |
| 12 | `DecisionRecord` | **MATCH** | Lines 427-438. Fields: tick, action, r, k_mod, sphere_count. |
| 13 | `WorkSignature` | **MATCH** | Lines 358-368. Fields: intensity, rhythm, diversity, focus. |
| 14 | `ActivationZones` | **MATCH** | Lines 698-705. Fields: active_threshold, prune_threshold, capacity. |

**EXTRA type not in D7:** `SphereFieldContext` (line 373) — global_r, my_cluster_size, is_synchronized, my_coupling_strength, tunnel_count.

### Public Functions (12 claimed)

| # | D7 Claim | Verdict |
|---|----------|---------|
| 1 | `PaneId::new(impl Into<String>) -> Self` | **MATCH** |
| 2 | `PaneId::as_str() -> &str` | **MATCH** |
| 3 | `TaskId::new() -> Self` (UUID v4) | **MATCH** |
| 4 | `TaskId::from_existing(impl Into<String>) -> Self` | **MATCH** |
| 5 | `Point3D::from_spherical(theta, phi) -> Self` | **MATCH** |
| 6 | `Point3D::angular_distance(self, other) -> f64` | **MATCH** |
| 7 | `Point3D::slerp(self, other, t) -> Self` | **MATCH** |
| 8 | `PaneSphere::new(id, persona) -> Self` | **MATCH** |
| 9 | `FleetMode::from_count(usize) -> Self` | **MATCH** |
| 10 | `now_secs() -> f64` | **MATCH** |
| 11 | `phase_diff(a, b) -> f64` | **MATCH** |
| 12 | `semantic_phase_region(tool_name) -> f64` | **MATCH** |

**EXTRA public functions not in D7 (16):**
`Point3D::new`, `Point3D::north`, `Point3D::dot`, `Point3D::norm`, `Point3D::normalized`, `Point3D::arc_distance`, `OrderParameter::new`, `OrderParameter::incoherent`, `TaskId::as_str`, `SphereMemory::new`, `Buoy::new`, `Buoy::boost_at`, `Buoy::drift_toward`, `Buoy::drift_home`, `FleetMode::name`, `ActivationZones::standard`

### Test Count

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 52 | 52 | **MATCH** |

---

## m02_error_handling (596 LOC)

### Types (4 claimed)

| # | D7 Claim | Verdict | Detail |
|---|----------|---------|--------|
| 1 | `PvError` enum "15+ variants" | **MISMATCH** | 27 actual variants. "15+" technically true but drastically understates. Omits Generic category. |
| 2 | `PvResult<T>` = `Result<T, PvError>` | **MATCH** | Line 26. |
| 3 | `ErrorSeverity`: Low, Medium, High, Critical | **MISMATCH** | Lines 172-182. Actual: **Info, Warning, Error, Critical**. 3 of 4 names wrong. |
| 4 | `ErrorClassifier`: `severity()`, `is_retryable()`, `error_code()` | **MISMATCH** | Lines 200-221. Method is **`code()`**, not `error_code()`. Other 2 match. |

### PvError Variants (16 claimed by D7)

| D7 Claimed | Verdict | Notes |
|------------|---------|-------|
| `ConfigLoad` | **MATCH** | `ConfigLoad(String)` |
| `ConfigValidation` | **MATCH** | `ConfigValidation(String)` |
| `NonFinite` | **MATCH** | `NonFinite { field, value }` |
| `OutOfRange` | **MATCH** | `OutOfRange { field, value, min, max }` |
| `EmptyString` | **MATCH** | `EmptyString { field }` |
| `StringTooLong` | **MATCH** | `StringTooLong { field, len, max }` |
| `InvalidChars` | **MATCH** | `InvalidChars { field, reason }` |
| `FieldComputation` | **MATCH** | `FieldComputation(String)` |
| `BridgeUnreachable` | **MATCH** | `BridgeUnreachable { service, url }` |
| `BridgeParse` | **MATCH** | `BridgeParse { service, reason }` |
| `BridgeError` | **MATCH** | `BridgeError { service, status }` |
| `BusProtocol` | **MATCH** | `BusProtocol(String)` |
| `BusTimeout` | **MISSING** | Does not exist. Bus has BusSocket/BusProtocol/BusTaskNotFound/CascadeRateLimit. |
| `PersistenceWrite` | **MISSING** | Does not exist. Persistence has Database/Snapshot. |
| `PersistenceRead` | **MISSING** | Does not exist. |
| `GovernanceRejected` | **MISSING** | Does not exist. Governance has ProposalNotFound/VotingClosed/QuorumNotReached. |

**EXTRA variants not in D7 (15):**
`SphereNotFound`, `SphereAlreadyRegistered`, `SphereCapReached`, `BridgeConsentDenied`, `BusSocket`, `BusTaskNotFound`, `CascadeRateLimit`, `Database`, `Snapshot`, `ProposalNotFound`, `VotingClosed`, `QuorumNotReached`, `Io`, `Json`, `Internal`

### Test Count

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 29 | 29 | **MATCH** |

---

## m03_config (654 LOC)

### Types (11 claimed)

| # | D7 Claim | Verdict | Detail |
|---|----------|---------|--------|
| 1 | `PvConfig` (10 sub-configs) | **MATCH** | Lines 26-49. All 10 sub-config fields present. |
| 2 | `ServerConfig`: port, bind_addr, body_limit_bytes | **MATCH** | Lines 126-133. All 3 fields exact. |
| 3 | `FieldConfig`: tick_interval_secs, kuramoto_dt, coupling_steps | **MISMATCH** | Lines 148-175. **13 fields** not 3. `coupling_steps` should be `coupling_steps_per_tick`. 10 undocumented fields. |
| 4 | `SphereConfig`: max_count, memory_cap, decay_rate | **MISMATCH** | Lines 200-219. **9 fields** not 3. `memory_cap`->`memory_max_count`, `decay_rate`->`decay_per_step`. |
| 5 | `CouplingConfig`: default_weight, auto_scale, k_min, k_max | **MISMATCH** | Lines 240-257. **8 fields** not 4. `auto_scale`->`auto_scale_k_period`, `k_min`->`strength_min`, `k_max`->`strength_max`. |
| 6 | `LearningConfig`: ltp_rate, ltd_rate, burst_multiplier | **MISMATCH** | Lines 277-288. **5 fields** not 3. `ltp_rate`->`hebbian_ltp`, `ltd_rate`->`hebbian_ltd`. |
| 7 | `BridgesConfig`: synthex_addr, me_addr, povm_addr, rm_addr, vms_addr | **MISMATCH** | Lines 305-328. **11 fields. ALL 5 `*_addr` fields DO NOT EXIST.** Actual: k_mod bounds + poll intervals. |
| 8 | `IpcConfig`: socket_path, subscribe_patterns, reconnect | **MISMATCH** | Lines 373-389. **7 fields** not ~3. No reconnect settings exist. |
| 9 | `PersistenceConfig`: snapshot_interval, wal_timeout | **MISMATCH** | Lines 413-422. **4 fields** not 2. `wal_timeout`->`wal_busy_timeout_ms`. |
| 10 | `ConductorConfig`: gain, breathing_blend | **MISMATCH** | Lines 351-358. **3 fields** not 2. Missing `divergence_cooldown_ticks`. |
| 11 | `GovernanceConfig`: "voting thresholds" | **PARTIAL** | Lines 438-445. 3 fields: proposal_voting_window_ticks, quorum_threshold, max_active_proposals. Vague but directionally correct. |

### Public Functions (2 claimed)

| # | D7 Claim | Verdict |
|---|----------|---------|
| 1 | `PvConfig::load() -> PvResult<Self>` | **MATCH** |
| 2 | `PvConfig::from_path(path) -> PvResult<Self>` | **MATCH** |

### Figment Usage

| D7 Claim | Verdict |
|----------|---------|
| Figment-based TOML + env overlay | **MATCH** — confirmed `Figment::new().merge(Toml::file(...)).merge(Env::prefixed("PV2_"))` |

### Test Count

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 25 | 25 | **MATCH** |

---

## Consolidated Scorecard

### Per-Module

| Module | Types Match | Types Mismatch | Fns Match | Fns Mismatch | Variants Match | Variants Missing | Variants Extra | Tests |
|--------|------------|---------------|-----------|-------------|---------------|-----------------|---------------|-------|
| m01 | 11 | 3 | 12 | 0 | — | — | — | 52=52 MATCH |
| m02 | 1 | 3 | — | — | 12 | 4 | 15 | 29=29 MATCH |
| m03 | 2 | 8 | 2 | 0 | — | — | — | 25=25 MATCH |
| **Total** | **14** | **14** | **14** | **0** | **12** | **4** | **15** | **3/3** |

### D7 Defect Severity Ranking

| Rank | Defect | Severity |
|------|--------|----------|
| 1 | **m03 `BridgesConfig`**: 5 `*_addr` fields fabricated, do not exist | CRITICAL — would mislead any consumer |
| 2 | **m01 `FleetMode`**: 3 of 5 claimed variants fabricated (Medium/Large/Massive) | CRITICAL — enum shape completely wrong |
| 3 | **m01 `FieldAction`**: 4 of 5 claimed variants fabricated (Steady/BoostK/ReduceK/KickPhases) | CRITICAL — actual has 8 different variants |
| 4 | **m02 `ErrorSeverity`**: 3 of 4 variant names wrong | HIGH — Info/Warning/Error not Low/Medium/High |
| 5 | **m02 PvError variants**: 4 MISSING + 15 EXTRA | HIGH — D7 lists 16, actual has 27 |
| 6 | **m03 field counts**: 8 sub-configs under-reported (total ~30 claimed vs ~80 actual) | HIGH — field completeness ~37% |
| 7 | **m03 field names**: ~10 renamed fields across sub-configs | MEDIUM — directionally correct, names wrong |
| 8 | **m02 `ErrorClassifier`**: `code()` not `error_code()` | LOW — minor rename |
| 9 | **m01 `PaneStatus`**: Missing `Complete` variant | LOW — 3 of 4 correct |

### Reliability by Claim Category

| Category | Accuracy | Notes |
|----------|----------|-------|
| Test counts | **100%** | All 3 exact (52, 29, 25) |
| Type existence | **100%** | All 29 claimed types exist |
| Function signatures | **100%** | All 14 claimed functions correct |
| Enum variant names | **~50%** | Major fabrications in FleetMode, FieldAction, ErrorSeverity |
| Struct field names | **~40%** | Widespread renaming, especially in m03 config |
| Struct field completeness | **~37%** | D7 lists ~30 of ~80 actual fields |
| PvError variant coverage | **44%** | 12 of 27 actual variants listed |
