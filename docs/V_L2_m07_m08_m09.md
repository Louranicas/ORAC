# V_L2: D7 Verification — m07_ipc_client, m08_bus_types, m09_wire_protocol

> **Verified:** 2026-03-25 | **Method:** 3 subagent parallel source read + claim-by-claim comparison
> **Source:** `m07_ipc_client.rs` (981 LOC), `m08_bus_types.rs` (~700 LOC), `m09_wire_protocol.rs` (1003 LOC)
> **D7 claims from:** `docs/D7_MODULE_PURPOSE_GUIDE.md` Layer 2 section

---

## Scorecard

| Module | MATCH | MISMATCH | MISSING | EXTRA | Test Count |
|--------|-------|----------|---------|-------|------------|
| m07_ipc_client | 8 | 5 | 3 | 7 | **MISMATCH** D7:11 Actual:23 |
| m08_bus_types | 19 | 4 | 4 | 8 | 67 = 67 MATCH |
| m09_wire_protocol | 18 | 6 | 1 | 12 | 40 = 40 MATCH |
| **TOTAL** | **45** | **15** | **8** | **27** | **2/3 MATCH** |

---

## m07_ipc_client (981 LOC)

### Types (2 claimed)

| D7 Claim | Verdict | Detail |
|----------|---------|--------|
| `IpcClient` — Async connection manager | **MATCH** | `pub struct IpcClient` with socket_path, pane_id, state, session_id, subscriptions, writer, reader |
| `ConnectionState`: Disconnected, Connecting, Connected, Subscribing, Subscribed, Failed | **MISMATCH** | Actual has **3** variants: `Disconnected`, `Connecting`, `Connected`. D7 claims 6. `Subscribing`, `Subscribed`, `Failed` do not exist. |

### ConnectionState Variants

| D7 Claimed | Verdict |
|------------|---------|
| `Disconnected` | **MATCH** |
| `Connecting` | **MATCH** |
| `Connected` | **MATCH** |
| `Subscribing` | **MISSING** — does not exist (client stays Connected after subscribing) |
| `Subscribed` | **MISSING** — does not exist |
| `Failed` | **MISSING** — does not exist (failures revert to Disconnected) |

### Public API (10 claimed)

| # | D7 Claim | Actual | Verdict |
|---|----------|--------|---------|
| 1 | `IpcClient::new(pane_id) -> Self` | `pub fn new(pane_id: PaneId) -> Self` | **MATCH** |
| 2 | `IpcClient::with_socket_path(pane_id, path) -> Self` | `pub fn with_socket_path(pane_id: PaneId, socket_path: impl Into<PathBuf>) -> Self` | **MATCH** |
| 3 | `connect_with_backoff() -> PvResult<u32>` | `pub async fn connect_with_backoff(&mut self) -> PvResult<u32>` | **MISMATCH** — D7 omits `async` and `&mut self` |
| 4 | `subscribe(patterns) -> PvResult<usize>` | `pub async fn subscribe(&mut self, patterns: &[String]) -> PvResult<usize>` | **MISMATCH** — D7 omits `async` and `&mut self` |
| 5 | `recv_frame() -> PvResult<BusFrame>` | `pub async fn recv_frame(&mut self) -> PvResult<BusFrame>` | **MISMATCH** — D7 omits `async` and `&mut self` |
| 6 | `send_frame(frame) -> PvResult<()>` | `pub async fn send_frame(&mut self, frame: &BusFrame) -> PvResult<()>` | **MISMATCH** — D7 omits `async` and `&mut self` |
| 7 | `disconnect() -> PvResult<()>` | `pub async fn disconnect(&mut self) -> PvResult<()>` | **MISMATCH** — D7 omits `async` and `&mut self` |
| 8 | `session_id() -> Option<&str>` | `pub fn session_id(&self) -> Option<&str>` | **MATCH** |
| 9 | `connection_state() -> ConnectionState` | `pub const fn connection_state(&self) -> ConnectionState` | **MATCH** |
| 10 | `is_connected() -> bool` | `pub const fn is_connected(&self) -> bool` | **MATCH** |

### EXTRA Public Items Not in D7 (7)

| Item | Signature |
|------|-----------|
| `connect()` | `pub async fn connect(&mut self) -> PvResult<()>` — single-attempt connect |
| `reconnect()` | `pub async fn reconnect(&mut self) -> PvResult<u32>` — reset + backoff reconnect |
| `max_backoff_attempts()` | `pub const fn max_backoff_attempts() -> u32` — returns 10 |
| `socket_path()` | `pub fn socket_path(&self) -> &Path` |
| `pane_id()` | `pub fn pane_id(&self) -> &PaneId` |
| `subscriptions()` | `pub fn subscriptions(&self) -> &[String]` |
| `DEFAULT_SOCKET_PATH` | `pub const &str = "/run/user/1000/pane-vortex-bus.sock"` |

### Tests

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 11 | **23** | **MISMATCH** — actual count is more than double the claim |

---

## m08_bus_types (~700 LOC)

### BusFrame Enum — CRITICAL VERIFICATION

**D7 claims 10 variants.** Source module doc says **"BusFrame (11 kinds)"**. Actual: **11 variants.**

| # | Actual Variant | D7 Claimed As | Verdict |
|---|---------------|---------------|---------|
| 1 | `Handshake` | `Connect` | **MISMATCH** — renamed |
| 2 | `Welcome` | `Welcome` | **MATCH** |
| 3 | `Subscribe` | `Subscribe` | **MATCH** |
| 4 | `Subscribed` | `Subscribed` | **MATCH** |
| 5 | `Submit` | `SubmitTask` | **MISMATCH** — renamed |
| 6 | `TaskSubmitted` | *(not claimed)* | **EXTRA** — D7 omits |
| 7 | `Event` | `Event` | **MATCH** |
| 8 | `Cascade` | *(not claimed)* | **EXTRA** — D7 omits |
| 9 | `CascadeAck` | *(not claimed)* | **EXTRA** — D7 omits |
| 10 | `Disconnect` | `Disconnect` | **MATCH** |
| 11 | `Error` | `Error` | **MATCH** |
| — | *(none)* | `TaskUpdate` | **MISSING** — fabricated by D7 |
| — | *(none)* | `Keepalive` | **MISSING** — fabricated by D7 |

**No separate `ClientFrame`/`ServerFrame` enums.** Direction is determined by `is_client_frame()` and `is_server_frame()` methods. `Cascade` and `CascadeAck` are bidirectional (return false from both).

### TaskTarget Enum

| D7 Claimed | Actual | Verdict |
|------------|--------|---------|
| `AnyIdle` | `AnyIdle` | **MATCH** |
| `FieldDriven` | `FieldDriven` | **MATCH** |
| `Willing` | `Willing` | **MATCH** |
| `Specific(PaneId)` | `Specific { pane_id: PaneId }` | **MATCH** (struct vs tuple — cosmetic) |

**4/4 MATCH**

### TaskStatus Enum

| D7 Claimed | Actual | Verdict |
|------------|--------|---------|
| `Pending` | `Pending` | **MATCH** |
| `Claimed` | `Claimed` | **MATCH** |
| `Running` | *(does not exist)* | **MISSING** — fabricated |
| `Completed` | `Completed` | **MATCH** |
| `Failed` | `Failed` | **MATCH** |
| `Requeued` | *(does not exist)* | **MISSING** — fabricated (`requeue()` resets to Pending) |

**D7 claims 6 variants. Actual: 4.** `Running` and `Requeued` do not exist.

### BusTask Struct

| D7 Claimed Field | Verdict |
|-----------------|---------|
| `description` | **MATCH** |
| `target` | **MATCH** |
| `status` | **MATCH** |
| `submitted_by` | **MATCH** |
| `claimed_by` | **MATCH** |

**EXTRA fields (5):** `id: TaskId`, `submitted_at: f64`, `claimed_at: Option<f64>`, `completed_at: Option<f64>`, `requeue_count: u32`

### BusEvent Struct

| D7 Claimed Field | Verdict |
|-----------------|---------|
| `event_type` | **MATCH** |
| `data` (JSON Value) | **MATCH** |
| `tick` | **MATCH** |
| `source` | **MISSING** — does not exist |

**EXTRA field:** `timestamp: f64`

### Public Methods (12 claimed)

| Method | Verdict |
|--------|---------|
| `BusTask::new(description, target, submitted_by) -> Self` | **MATCH** |
| `BusTask::claim(claimer) -> bool` | **MATCH** |
| `BusTask::complete()` | **MATCH** |
| `BusTask::fail()` | **MATCH** |
| `BusTask::requeue()` | **MATCH** |
| `BusEvent::new(event_type, data, tick) -> Self` | **MATCH** |
| `BusEvent::matches_pattern(pattern) -> bool` | **MATCH** |
| `BusFrame::to_ndjson() -> Result<String>` | **MATCH** |
| `BusFrame::from_ndjson(line) -> Result<Self>` | **MATCH** |
| `BusFrame::is_client_frame()` | **MATCH** |
| `BusFrame::is_server_frame()` | **MATCH** |
| `BusFrame::frame_type()` | **MATCH** |

**12/12 MATCH.** EXTRA methods (4): `BusTask::is_pending`, `BusTask::is_terminal`, `BusTask::elapsed_secs`, `BusEvent::text`.

### Tests

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 67 | 67 | **MATCH** |

---

## m09_wire_protocol (1003 LOC)

### ProtocolState Enum — CRITICAL VERIFICATION

**D7 claims 6 states.** Actual: **6 variants.** Count is correct but one name is wrong.

| D7 Claimed | Actual | Verdict |
|------------|--------|---------|
| `Disconnected` | `Disconnected` | **MATCH** |
| `Handshaking` | `Handshaking` | **MATCH** |
| `Connected` | `Connected` | **MATCH** |
| `Subscribing` | `Subscribing` | **MATCH** |
| `Active` | `Active` | **MATCH** |
| `Error` | **`Closing`** | **MISMATCH** — D7 says `Error`, actual is `Closing` ("Connection closing after Disconnect sent") |

**6 variants confirmed, but 6th is `Closing` not `Error`. No `Error` state exists.**

### FrameValidation Enum

| D7 Claimed | Actual | Verdict |
|------------|--------|---------|
| `Valid(BusFrame)` | `Valid` (unit variant, no payload) | **MISMATCH** — no BusFrame payload |
| `Invalid(String)` | `UnexpectedFrame { frame_type: String, state: ProtocolState }` | **MISMATCH** — different name, struct variant with 2 fields |
| `Duplicate` | *(does not exist)* | **MISSING** — fabricated |
| `Oversized` | `Oversized { size: usize }` | **MISMATCH** — D7 shows unit variant, actual has size field |
| *(not listed)* | `Malformed { reason: String }` | **EXTRA** — exists, absent from D7 |

**4 variants in source, 4 claimed by D7. But 3 of 4 are wrong in name or shape, `Duplicate` doesn't exist, and `Malformed` is unlisted.**

### WireStats Fields

| D7 Claimed | Actual | Verdict |
|------------|--------|---------|
| `frames_sent` | `frames_sent: u64` | **MATCH** |
| `frames_received` | `frames_received: u64` | **MATCH** |
| `bytes_sent` | `bytes_sent: u64` | **MATCH** |
| `bytes_received` | `bytes_received: u64` | **MATCH** |
| `errors` | `validation_errors: u64` | **MISMATCH** — actual name is `validation_errors` |

**EXTRA fields (2):** `keepalives_sent: u64`, `state_transitions: u64`

### Public Methods (10 claimed by D7)

| Method | Verdict |
|--------|---------|
| `WireProtocol::new(pane_id) -> Self` | **MATCH** |
| `initiate_handshake() -> PvResult<()>` | **MATCH** |
| `subscribe(patterns) -> PvResult<()>` | **MATCH** |
| `submit_task(task) -> PvResult<()>` | **MATCH** |
| `process_incoming(raw) -> PvResult<FrameValidation>` | **MATCH** |
| `dequeue_send() -> Option<String>` | **MATCH** |
| `dequeue_recv() -> Option<BusFrame>` | **MATCH** |
| `needs_keepalive(now) -> bool` | **MATCH** |
| `send_keepalive() -> PvResult<()>` | **MATCH** |
| `force_reset()` | **MATCH** |

**10/10 MATCH.**

### EXTRA Public Methods Not in D7 (9)

`with_keepalive(pane_id, keepalive_secs)`, `state()`, `session_id()`, `subscriptions()`, `stats()`, `send_queue_len()`, `recv_buffer_len()`, `drain_recv()`, `disconnect(reason)`

### Constants (3 claimed)

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| `PROTOCOL_VERSION = "2.0"` | `pub const PROTOCOL_VERSION: &str = "2.0"` | **MATCH** |
| `MAX_FRAME_SIZE = 65536` | `pub const MAX_FRAME_SIZE: usize = 65_536` | **MATCH** |
| `DEFAULT_KEEPALIVE_SECS = 30` | `pub const DEFAULT_KEEPALIVE_SECS: u64 = 30` | **MATCH** |

### Tests

| D7 Claim | Actual | Verdict |
|----------|--------|---------|
| 40 | 40 | **MATCH** |

---

## Critical D7 Inaccuracies Summary

### Fabricated Items

| Module | Fabricated | What Actually Exists |
|--------|-----------|---------------------|
| m07 | `ConnectionState::Subscribing` | Client stays in `Connected` after subscribe |
| m07 | `ConnectionState::Subscribed` | Does not exist |
| m07 | `ConnectionState::Failed` | Failures revert to `Disconnected` |
| m08 | `BusFrame::Connect` | Actual: `BusFrame::Handshake` |
| m08 | `BusFrame::SubmitTask` | Actual: `BusFrame::Submit` |
| m08 | `BusFrame::TaskUpdate` | Does not exist |
| m08 | `BusFrame::Keepalive` | Does not exist |
| m08 | `TaskStatus::Running` | Does not exist |
| m08 | `TaskStatus::Requeued` | Does not exist (requeue resets to Pending) |
| m08 | `BusEvent.source` field | Does not exist |
| m09 | `ProtocolState::Error` | Actual: `ProtocolState::Closing` |
| m09 | `FrameValidation::Duplicate` | Does not exist |
| m09 | `FrameValidation::Invalid(String)` | Actual: `UnexpectedFrame { frame_type, state }` |
| m09 | `WireStats.errors` | Actual: `WireStats.validation_errors` |

### Systematic D7 Omission: `async` Keyword

D7 consistently omits `async` from all IPC client method signatures. All 5 I/O methods on `IpcClient` are `async fn` — critical for callers.

### Coverage Gaps

| Module | Unlisted Items |
|--------|---------------|
| m07 | 6 public methods, 1 constant, 12 tests |
| m08 | 3 BusFrame variants (TaskSubmitted, Cascade, CascadeAck), 5 BusTask fields, 4 methods |
| m09 | 9 public methods, 1 FrameValidation variant, 2 WireStats fields |

### Reliability by Claim Category

| Category | Accuracy |
|----------|----------|
| Test counts | **67%** — m08 and m09 exact, m07 wildly wrong (11 vs 23) |
| Method existence | **100%** — all 32 claimed methods exist |
| Type existence | **100%** — all claimed types exist |
| Enum variant names | **~55%** — widespread renaming and fabrication |
| Enum variant count | **~33%** — ConnectionState (3 not 6), TaskStatus (4 not 6), BusFrame (11 not 10) |
| Struct field names | **~80%** — mostly correct, some renames |
| Constant values | **100%** — all 3 exact |
